// 描画に使用する共有定数

// ---------------------------------------------------------------------------
// デバッグフラグ
// ---------------------------------------------------------------------------

/** trueにするとSCB全体の緑点線枠を表示 */
export const SHOW_SCB_DEBUG_BOUNDS = false

/** trueにするとS1, T1, P1, T2, P2のラベルを表示 */
export const SHOW_REFERENCE_POINT_LABELS = false

/** trueにすると衝突判定用ポリゴンを赤い線で表示 */
export const SHOW_COLLISION_POLYGONS = false

// ---------------------------------------------------------------------------
// SCB領域の塗りつぶし色
// ---------------------------------------------------------------------------

/** SD（Safety Distance）: 赤色 */
export const SD_COLOR = 'rgb(255, 0, 0)'

/** SCB1: オレンジ */
export const SCB1_COLOR = 'rgb(255, 178, 102)'

/** SCB2: 黄色 */
export const SCB2_COLOR = 'rgb(255, 255, 0)'

/** SCB3: 水色 */
export const SCB3_COLOR = 'rgb(102, 178, 255)'

// ---------------------------------------------------------------------------
// 車両本体の枠線色
// ---------------------------------------------------------------------------

/** 車両枠線の外側（黒） */
export const VEHICLE_OUTER_STROKE_COLOR = '#000000'

/** 車両枠線の内側（白） */
export const VEHICLE_INNER_STROKE_COLOR = '#FFFFFF'

// ---------------------------------------------------------------------------
// 自車両の塗りつぶし色
// ---------------------------------------------------------------------------

/** 自車両の塗りつぶし色（赤） */
export const EGO_VEHICLE_COLOR = '#FF4444'

// ---------------------------------------------------------------------------
// 対象車両の塗りつぶし色（複数車両に対応するパレット）
// ---------------------------------------------------------------------------

/** 対象車両のメインカラーパレット */
export const TARGET_VEHICLE_COLORS = [
  '#4444FF', // 青
  '#44FF44', // 緑
  '#FF44FF', // マゼンタ
  '#00CED1', // ダークターコイズ
  '#FFD700', // ゴールド
]

// ---------------------------------------------------------------------------
// 軌跡線の色
// ---------------------------------------------------------------------------

/** 自車両の過去軌跡（濃い赤） */
export const EGO_TRAJECTORY_COLOR_PAST = '#FF3333'

/** 自車両の未来軌跡（薄い赤） */
export const EGO_TRAJECTORY_COLOR_FUTURE = '#FF9999'

/** 対象車両の過去軌跡パレット（各車両色と同系統の濃い色） */
export const TARGET_TRAJECTORY_COLORS_PAST = [
  '#3333FF', // 青
  '#33FF33', // 緑
  '#CC33CC', // マゼンタ
  '#00A0A0', // ターコイズ
  '#B8A800', // ゴールド
]

/** 対象車両の未来軌跡パレット（各色の薄いバージョン） */
export const TARGET_TRAJECTORY_COLORS_FUTURE = [
  '#9999FF', // 薄い青
  '#99FF99', // 薄い緑
  '#FF99FF', // 薄いマゼンタ
  '#66E5E5', // 薄いターコイズ
  '#FFE866', // 薄いゴールド
]

// ---------------------------------------------------------------------------
// 参照点マーカーの色
// ---------------------------------------------------------------------------

/** S1: 自車OBB最近点（ライムグリーン、dy系統） */
export const MARKER_S1_COLOR = '#32CD32'

/** T1: 対象車OBB最近点（トマト色、dx系統） */
export const MARKER_T1_COLOR = '#FF6347'

/** P1: 軌跡上のT1最近点（クリムゾン、dx系統） */
export const MARKER_P1_COLOR = '#DC143C'

/** T2: S1から対象車OBBへの垂線の足（フォレストグリーン、dy系統） */
export const MARKER_T2_COLOR = '#228B22'

/** P2: 軌跡上のT2最近点（ミディアムシーグリーン、dy系統） */
export const MARKER_P2_COLOR = '#3CB371'
