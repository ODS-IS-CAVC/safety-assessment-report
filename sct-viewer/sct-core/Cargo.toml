[package]
name = "sct-core"
version = "0.1.0"
edition = "2021"
description = "SCT計算エンジン（プリビルドバイナリラッパー）"

[lib]
crate-type = ["rlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
libloading = "0.8"
